export interface CampaignCardType {
  title: string;
  description: string;
  image: string;
  goal: Number;
  raised: Number;
}

export interface CauseCardType {
  icon: JSX.Element;
  title: string;
}
